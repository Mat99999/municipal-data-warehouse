# Security policy

This repository processes public aggregate data and requires no source credentials. Do not commit `.env`, database dumps, or future private source tokens. Report a vulnerability privately through GitHub's security advisory feature rather than a public issue.

Development passwords in `.env.example` are local-only defaults and must be replaced in any shared deployment.

